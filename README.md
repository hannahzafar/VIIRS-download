# VIIRS-download
Download VIIRS data via earthaccess Python library

This project was developed using Pixi for package management, but an environment.yml is provided for conda users.

viirs_download.py contains the Python scripts for employing earthaccess to query and download VIIRS files. 
viirs_download_wrapper.sh is a bash script that is used for simplifying/guiding the command line run arguments.
